# Data artifacts — NEVER commit raw/intermediate data (source dump is 32 GB)
*.dmp
*.duckdb
*.duckdb.wal
data/duckdb/
data/out/
# NOTE: the *demo* parquet subset (data/public/*.parquet) is small and SHOULD
# be committed for GitHub Pages. Adjust this if you put it elsewhere.
data/scratch/

# Node / Vite
node_modules/
frontend/dist/
.env
.env.local

# Python tooling (if any)
venv/
env/
__pycache__/
*.pyc

# Editor / OS
.idea/
.DS_Store
.vscode/
frontend/public/data
